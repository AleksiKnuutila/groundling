[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "groundling"
version = "0.0.1"
description = "Grounded Q&A over PDFs with clickable citation viewers."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "pymupdf>=1.24",
  "jinja2>=3.1",
  "typer>=0.12",
  "markdown-it-py>=3.0",
]

[dependency-groups]
# anthropic + python-dotenv are only needed by skill/api_smoke*.py — the
# end-to-end smoke tests that exercise the Claude Skill via the real API.
dev = [
  "pytest>=8.0",
  "anthropic>=0.40",
  "python-dotenv>=1.0",
]

[project.scripts]
groundling = "groundling.cli:app"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.hatch.build.targets.wheel]
packages = ["src/groundling"]
